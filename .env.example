# Motion Studio - Environment Variables Template
# Copy this file to .env.local for local development.
# In production, the API key is stored encrypted via electron-store and never read from .env.

# Optional: For development convenience only. Production app uses Settings UI to store API key.
# GEMINI_API_KEY=your_gemini_api_key_here
